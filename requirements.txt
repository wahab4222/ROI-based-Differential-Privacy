# Core runtime dependencies (portable)
numpy>=1.20
scikit-learn>=1.0
Pillow>=9.0
flwr>=1.0

# PyTorch stack
# Note: install the correct torch/torchvision build (CPU/CUDA) from https://pytorch.org/get-started/locally/
torch>=2.0
torchvision>=0.15
